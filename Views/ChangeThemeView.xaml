<UserControl x:Class="WPF_MVVM_SPA_Template.Views.ChangeThemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
             mc:Ignorable="d">
    <Grid Background="{DynamicResource MainContentBackground}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Change Theme" FontWeight="Bold" FontSize="16" />
        <TextBlock Grid.Row="1" Text="Push this button to change the theme" FontSize="14" />
        <Button Grid.Row="2" Content="Change Theme Now!" Command="{Binding ChangeThemeCommand}"  Margin="0,10,0,0" />
    </Grid>
</UserControl>
